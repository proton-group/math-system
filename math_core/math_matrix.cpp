#include "math_matrix.h"

matrix operator+(matrix f_matrix, matrix s_matrix) {
    assert(f_matrix.xsize == s_matrix.xsize & f_matrix.ysize == s_matrix.ysize);
    for(int i; i <= f_matrix.xsize * f_matrix.ysize; i++) {
        int rank = 0;
        while(f_matrix.data[i] != " "){
            
        }
    }
}